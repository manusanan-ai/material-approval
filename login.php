<?php require 'config.php';if(user())redirect('index.php');$error='';if($_SERVER['REQUEST_METHOD']==='POST'){check_csrf();$s=db()->prepare("SELECT * FROM users WHERE username=? AND active=1");$s->execute([trim($_POST['username']??'')]);$u=$s->fetch();if($u&&password_verify($_POST['password']??'',$u['password_hash'])){session_regenerate_id(true);$_SESSION['user']=$u;redirect('index.php');}$error=current_lang()==='th'?'ชื่อผู้ใช้หรือรหัสผ่านไม่ถูกต้อง':'Invalid username or password.';}?><!doctype html><html lang="<?=e(current_lang())?>"><head><?php include 'partials/head.php';?></head><body><div class="login-page"><section class="login-brand"><div class="login-logo">▣</div><h1>Material Flow</h1><p><?=current_lang()==='th'?'ระบบบริหารจัดการเบิกจ่ายพัสดุ พร้อมกระบวนการอนุมัติหลายระดับ ช่วยให้การจัดการวัสดุเป็นระบบ ตรวจสอบง่าย และลดงานเอกสาร':'A modern material requisition and multi-level approval workflow that keeps inventory, requests and decisions in one place.'?></p><div class="feature-list"><div>✓ <?=current_lang()==='th'?'ติดตามคำขอเบิกได้ทุกขั้นตอน':'Track every requisition step'?></div><div>✓ <?=current_lang()==='th'?'อนุมัติตามลำดับสิทธิ์ของผู้ใช้งาน':'Multi-level approval by role'?></div><div>✓ <?=current_lang()==='th'?'ควบคุมสต็อกและบันทึกการจ่ายพัสดุ':'Inventory control and issue history'?></div></div></section><section class="login-side"><div style="width:min(430px,100%);display:flex;justify-content:flex-end;margin-bottom:12px"><div class="lang-switch"><a class="<?=current_lang()==='th'?'active':''?>" href="login.php?lang=th">🇹🇭 TH</a><a class="<?=current_lang()==='en'?'active':''?>" href="login.php?lang=en">🇬🇧 EN</a></div></div><div class="login-card"><h2><?=e(t('login'))?></h2><div class="sub"><?=e(t('login_sub'))?></div><?php if($error):?><div class="alert error"><?=e($error)?></div><?php endif;?><form method="post" style="padding:0;border:0;box-shadow:none;background:transparent"><input type="hidden" name="csrf" value="<?=e(csrf())?>"><div class="field"><label><?=e(t('username'))?></label><input name="username" placeholder="<?=e(t('username'))?>" required autofocus></div><div class="field"><label><?=e(t('password'))?></label><input type="password" name="password" placeholder="<?=e(t('password'))?>" required></div><button class="submit" type="submit"><?=e(t('login'))?> →</button></form><div class="demo-box"><b><?=current_lang()==='th'?'บัญชีทดลอง':'Demo accounts'?></b><br>admin / password · requester / password · head / password<br>procure / password · director / password · store / password</div><div class="login-link"><?=current_lang()==='th'?'ติดต่อผู้ดูแลระบบเพื่อขอสร้างบัญชี':'Contact the administrator to request an account.'?> · <a href="register.php">สร้างบัญชี / Register</a></div></div></section></div></body></html>
